<script>
  /**
   * 应用级别的配置，供所有页面公用
   */
  import { $util, $apis } from './helper'

  export default {
    $apis: $apis,
    showMenu: $util.showMenu,
    createShortcut: $util.createShortcut
  }
</script>